[build]
builder = "nixpacks"
buildCommand = "npm install && rm -rf node_modules/.cache && npm run build"

[deploy]
startCommand = "serve dist -l tcp://0.0.0.0:$PORT"
healthcheckPath = "/"
restartPolicyType = "on_failure"
